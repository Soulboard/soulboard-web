# Solana RPC endpoint
RPC_URL=https://api.devnet.solana.com

# Path to your wallet keypair JSON file
# Default: ~/.config/solana/phantom.json
# WALLET_PATH=/root/.config/solana/phantom.json

# ThingSpeak API URL for fetching metrics
THINGSPEAK_URL=https://api.thingspeak.com/channels/2890626/feeds.json?results=11

# Location public key (required)
LOCATION_PUBKEY=

# Device index (optional - will auto-register if not specified)
# DEVICE_IDX=0

# Device authority public key (optional - defaults to wallet public key)
# DEVICE_AUTHORITY=

# Allow automatic device registration (default: true)
ALLOW_REGISTER=true

# Polling interval in milliseconds (default: 120000 = 2 minutes)
POLL_INTERVAL_MS=120000

# Path to store oracle state
STATE_PATH=/data/oracle-state.json
